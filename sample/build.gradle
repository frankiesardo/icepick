apply plugin: 'android'

dependencies {
    compile project(':icepick')
}

android {
    compileSdkVersion 17
    buildToolsVersion '18.0.1'

    defaultConfig {
        minSdkVersion 14
        targetSdkVersion 16
    }
}
