subprojects {

    group = 'com.github.frankiesardo'
    version = "2.0.1-SNAPSHOT"

    buildscript {
        repositories {
            mavenCentral()
        }
        dependencies {
            classpath 'com.android.tools.build:gradle:0.5.4'
            classpath 'org.gradle.api.plugins:gradle-nexus-plugin:0.2'
        }
    }
    repositories {
        mavenCentral()
    }
}
